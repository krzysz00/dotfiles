General rules on interacting with git repositories:

- When committing changes, add yourself as a co-author with a `Co-Authored-By`
- When addressing review feedback or making fixes, make new commits instead of `--amend`ing - I'll rewrite history afterwards
